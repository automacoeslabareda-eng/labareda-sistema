# Environment & Secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies
node_modules/

# Build & Logs
dist/
build/
*.log
logs/

# IDE & OS
.DS_Store
Thumbs.db
.idea/
*.swp
Nova pasta/

# AIOX Local
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
.aiox/handoffs/

# Local Netlify folder
.netlify

# Arquivos de design (PSD/AI/pasta de identidade) — grandes demais para o GitHub
# (limite de 100MB/arquivo). Ficam de backup no PC + OneDrive.
IDENTIDADE VISUAL/
*.psd
*.ai

# Imagens-fonte cruas (já processadas em src/img/) e locks do LibreOffice
Imagens/
.~lock.*#
